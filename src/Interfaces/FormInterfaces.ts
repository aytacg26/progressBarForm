export interface sendData {
  name: string;
  surname: string;
  email: string;
  currentJob: string;
  birthData: { month: string; year: string };
}

export interface formData {
  name: string;
  surname: string;
  email: string;
  currentJob: string;
  month: string;
  year: string;
}
